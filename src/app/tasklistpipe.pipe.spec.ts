import { TasklistpipePipe } from './tasklistpipe.pipe';

describe('TasklistpipePipe', () => {
  it('create an instance', () => {
    const pipe = new TasklistpipePipe();
    expect(pipe).toBeTruthy();
  });
});
